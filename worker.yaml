apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: voting-app
  labels:
    app: worker
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  strategy: {}
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - image: dockersamples/examplevotingapp_worker
        name: examplevotingapp-worker-8hvcj
        resources: {}
status: {}
